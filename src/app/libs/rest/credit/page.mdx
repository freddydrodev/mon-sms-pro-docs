---
sidebarTitle: Crédits 💰
title: Gestion des Crédits | API REST Mon SMS PRO
description: Guide complet pour gérer vos crédits SMS avec notre API REST. Consultez votre solde, suivez vos transactions et gérez vos crédits via des requêtes HTTP.
keywords: crédits SMS, API REST crédits, solde SMS, API SMS, endpoints crédits, transactions SMS, facturation SMS
---

import { Callout, Table } from "nextra/components";

# Gestion des Crédits

Les crédits sont l'unité de facturation pour l'envoi de SMS. Notre API REST vous permet de consulter votre solde et de suivre vos transactions.

## Fonctionnalités

- Consultation du solde
- Suivi des transactions
- Historique des dépenses
- Détails des facturations
- Alertes de solde
- Rapports financiers

## Utilisation

Après avoir [initialisé](/libs/rest/get-started/#init) votre API, vous pouvez utiliser les endpoints suivants pour gérer vos crédits.

### Consultation du solde

```bash
POST /v1/credit/balance
Content-Type: application/json

{
  "apiKey": "YOUR_API_KEY" // Required
}
```

#### Réponse

```json
{
  "success": true,
  "data": {
    "balance": 1000,
    "currency": "XOF"
  },
  "error": null
}
```

### Liste des transactions

```bash
POST /v1/credit/transactions
Content-Type: application/json

{
  "apiKey": "YOUR_API_KEY", // Required
  "orderBy": "createdAt",   // Optional: "createdAt" | "amount"
  "page": 1,                // Optional: defaults to 1
  "limit": 10               // Optional: defaults to 10
}
```

#### Réponse

```json
{
  "success": true,
  "data": {
    "transactions": [
      {
        "id": "txn_123",
        "type": "DEBIT",
        "amount": 50,
        "description": "Campagne SMS - 100 messages",
        "createdAt": "2024-03-20T10:00:00Z",
        "campaignId": "camp_123"
      }
    ],
    "pagination": {
      "total": 100,
      "page": 1,
      "limit": 10,
      "pages": 10
    }
  },
  "error": null
}
```

<Callout type="info">
  Toutes les réponses de l'API suivent le format standard : `{ success: boolean, data: any, error: any }`.
  Le champ `data` contient les données de la réponse, tandis que `error` contient les détails en cas d'erreur.
</Callout>

## Documentation de l'API

### POST /v1/credit/balance

Récupère le solde actuel de crédits.

<Table style={{ marginTop: 20 }}>
  <thead>
    <Table.Tr>
      <Table.Th>Nom</Table.Th>
      <Table.Th>Description</Table.Th>
      <Table.Th>Type</Table.Th>
      <Table.Th>Facultatif</Table.Th>
      <Table.Th>Valeur par défaut</Table.Th>
    </Table.Tr>
  </thead>
  <tbody>
    <Table.Tr>
      <Table.Td>apiKey</Table.Td>
      <Table.Td>Clé API</Table.Td>
      <Table.Td>String</Table.Td>
      <Table.Td>Non</Table.Td>
      <Table.Td>-</Table.Td>
    </Table.Tr>
  </tbody>
</Table>

### POST /v1/credit/transactions

Récupère la liste des transactions avec pagination et tri.

<Table style={{ marginTop: 20 }}>
  <thead>
    <Table.Tr>
      <Table.Th>Nom</Table.Th>
      <Table.Th>Description</Table.Th>
      <Table.Th>Type</Table.Th>
      <Table.Th>Facultatif</Table.Th>
      <Table.Th>Valeur par défaut</Table.Th>
    </Table.Tr>
  </thead>
  <tbody>
    <Table.Tr>
      <Table.Td>apiKey</Table.Td>
      <Table.Td>Clé API</Table.Td>
      <Table.Td>String</Table.Td>
      <Table.Td>Non</Table.Td>
      <Table.Td>-</Table.Td>
    </Table.Tr>
    <Table.Tr>
      <Table.Td>orderBy</Table.Td>
      <Table.Td>Champ de tri</Table.Td>
      <Table.Td>"createdAt" | "amount"</Table.Td>
      <Table.Td>Oui</Table.Td>
      <Table.Td>createdAt</Table.Td>
    </Table.Tr>
    <Table.Tr>
      <Table.Td>page</Table.Td>
      <Table.Td>Numéro de page</Table.Td>
      <Table.Td>Number</Table.Td>
      <Table.Td>Oui</Table.Td>
      <Table.Td>1</Table.Td>
    </Table.Tr>
    <Table.Tr>
      <Table.Td>limit</Table.Td>
      <Table.Td>Nombre d'éléments par page</Table.Td>
      <Table.Td>Number</Table.Td>
      <Table.Td>Oui</Table.Td>
      <Table.Td>10</Table.Td>
    </Table.Tr>
  </tbody>
</Table>

### Structure des transactions

```json
{
  "id": "string",          // Identifiant unique de la transaction
  "type": "string",        // Type de transaction: "CREDIT" | "DEBIT"
  "amount": number,        // Montant de la transaction
  "description": "string", // Description de la transaction
  "createdAt": "string",   // Date de création (ISO 8601)
  "campaignId": "string"   // ID de la campagne associée (pour les débits)
}
```

### Exemple d'erreur

```json
{
  "success": false,
  "data": null,
  "error": {
    "code": "INVALID_API_KEY",
    "message": "Clé API invalide ou expirée"
  }
}
```

## Bonnes pratiques

1. **Surveillance** : Vérifiez régulièrement votre solde
2. **Alertes** : Configurez des alertes de solde bas
3. **Suivi** : Consultez régulièrement vos transactions
4. **Budget** : Définissez un budget mensuel
5. **Analyse** : Analysez vos dépenses par campagne
6. **Optimisation** : Optimisez vos envois pour réduire les coûts
7. **Facturation** : Gardez une trace des factures
8. **Rapports** : Générez des rapports financiers réguliers

## Cas d'utilisation

- Suivi des dépenses
- Gestion budgétaire
- Facturation clients
- Rapports financiers
- Audit des transactions
- Optimisation des coûts
- Planification budgétaire

## FAQ

### Comment consulter mon solde ?

Utilisez l'endpoint `/v1/credit/balance` pour obtenir votre solde actuel.

### Comment suivre mes transactions ?

Utilisez l'endpoint `/v1/credit/transactions` pour obtenir la liste de vos transactions.

### Comment sont facturés les SMS ?

Les SMS sont facturés selon le type de message et le nombre de segments.

### Puis-je obtenir un historique détaillé ?

Oui, l'endpoint `/v1/credit/transactions` vous permet d'obtenir un historique complet.

### Comment recharger mes crédits ?

Les recharges se font via votre compte Mon SMS PRO ou en contactant notre service commercial.

### Comment optimiser mes coûts ?

Utilisez des messages courts, évitez les caractères spéciaux et optimisez vos campagnes.
